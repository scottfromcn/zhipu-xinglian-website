import SiteLayout, { PageHero, SectionHeading, ActionLink, ContactCTA } from '@/components/SiteLayout';
import { industrialScenarios } from '@/data/business';

export default function IndustrialSolutionsPage() {
  return <SiteLayout><PageHero eyebrow="工业智能 / 场景解决方案" title="让工业 AI 围绕实际任务落地" description="基于 SaaS 或私有化一体机形式的工业智能体平台，在可信环境中连接企业数据、Agent 与专业工具，从具体任务验证业务价值。"><div className="hero-actions"><ActionLink to="/support/sales?business=industrial">沟通工业场景</ActionLink><ActionLink to="/industrial-platform" secondary>了解平台基础</ActionLink></div></PageHero>
    <section className="section-pad"><div className="site-shell"><SectionHeading eyebrow="重点场景" title="从业务问题出发，明确输入与交付" description="以下为场景方案与拓展方向；具体能力需结合数据、系统接口与业务流程评估。" /><div className="scenario-list">{industrialScenarios.map((item, index) => <article className="scenario-card" id={item.id} key={item.id}><div><span className="status-tag">{item.status}</span><p className="scenario-number">0{index + 1}</p><h3>{item.title}</h3><p>{item.desc}</p></div><dl><div><dt>业务输入</dt><dd>{item.input}</dd></div><div><dt>预期交付</dt><dd>{item.output}</dd></div><div><dt>落地边界</dt><dd>{item.boundary}</dd></div></dl></article>)}</div></div></section>
    <section className="section-pad section-tint"><div className="site-shell"><SectionHeading eyebrow="实施路径" title="先验证一个场景，再沉淀一类能力" /><div className="four-grid">{[['场景诊断', '明确业务问题、使用人群、现有流程与衡量标准。'], ['接入与配置', '梳理数据、系统、模型和权限，配置必要的技能与工具。'], ['试点与验收', '以真实任务验证结果质量、人工复核流程与使用成本。'], ['复用与推广', '将已验证的技能和组件沉淀到平台，按需扩展场景。']].map(([title, desc], index) => <article className="content-card" key={title}><span className="step-number">0{index + 1}</span><h3>{title}</h3><p>{desc}</p></article>)}</div></div></section><ContactCTA /></SiteLayout>;
}
