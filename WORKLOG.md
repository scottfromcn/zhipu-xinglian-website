# 官网工作日志

## 2026-09-20：启动官网本地预览

- 任务：运行现有官网并打开预览。
- 方案：复用已安装依赖，启动 Vite；浏览器验收首页与 TokenHub 页面，再保留首页供查看。
- 启动命令：在本目录执行 `npm run dev -- --host 127.0.0.1`。
- 访问地址：http://127.0.0.1:3000/ 。
- 验收结果：Vite 启动成功；浏览器确认首页标题、正文、背景及按钮正常显示；点击“了解 TokenHub”成功进入解决方案页；点击品牌链接成功返回首页。
- 当前状态：服务保持运行，预览页已打开；未修改业务代码。
- 已知现状：文档、定价、关于我们等路由仍使用 PlaceholderPage；联系方式及备案号存在占位内容，本次仅启动预览。

## 2026-09-21：官网调整为工业智能与 OfficeAI 两条主线

### 已确认方案与范围

- 用户确认：以“工业智能体平台 + 工业 AI 解决方案”和“OfficeAI 国企办公解决方案”为两条主线。
- OfficeAI 由 TokenHub、SkillHub、Agent Admin、OfficeAgent 四个模块组成。
- 执行：首页、导航、页脚统一口径；完善业务介绍页、客户案例、关于我们与演示准备页；兼容旧链接；完成浏览器验收与 Git 提交。
- 验收：首屏清楚呈现两条主线，四模块关系明确，页面口径一致，桌面/手机正常，构建与主要跳转通过。

### 已完成

- 首页主标题改为“让 AI 深入工业现场，走进国企办公”；两大业务入口同级展示。
- 新增 `/industrial-platform`、`/solutions/industrial`、`/office-ai`，展示平台供给与运行流程、工业场景方案、OfficeAI 四模块及协作流程。
- 工业平台内容参考 `/Users/jqxumbp/workspace/ai-industry-platform/WORKLOG.md` 和 `apps/web/src/main.jsx`；基础演示能力与架构/场景拓展方向分开表述，没有修改该项目。
- 客户案例根据本工作区既有日志中的苏轨项目记录呈现，区分既有办公专员实践与审计、IT 运维推进方向，不声称 OfficeAI 四模块已经整体交付。
- TAC 移至关于我们页；首页移除缺少依据的客户名单、数量、调用量及 SLA 数字；页脚移除电话与备案号占位文本。
- 原 TokenHub、制造业、交通、政务等业务链接映射到新页面或对应锚点。
- 演示准备页支持业务方向预选、需求清单生成、预览与文本导出；明确未接通在线预约、不发送表单信息。
- 增加中文页面语言、描述信息、页面标题、键盘焦点、手机导航和锚点定位。

### 自验结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| 生产构建 | 通过 | `npm run build`，TypeScript 与 Vite 成功 |
| 修改文件检查 | 通过 | 本次新增/修改的 TS/TSX 文件 ESLint 通过；`git diff --check` 通过 |
| 页面加载与布局 | 通过 | 7 个页面分别在 1440px 和 390px 浏览器视口检查；页面宽度等于视口，无横向溢出；每页仅 1 个 h1，无损坏图片 |
| 视觉检查 | 通过 | 查看桌面首页、OfficeAI 四卡片与流程，手机演示页、OfficeAI 首屏与模块卡片，内容无叠压 |
| 手机导航 | 通过 | 打开菜单后进入 OfficeAI，菜单自动关闭 |
| 模块锚点 | 通过 | 点击 SkillHub 页脚链接进入对应模块；模块顶部 104px，固定导航底部 73px，内容未被遮挡 |
| 旧链接兼容 | 通过 | 四个原业务页分别进入 OfficeAI、工业采购、工业运维和 OfficeAI，新锚点存在 |
| 演示清单 | 通过（生成/内容） | OfficeAI 入口自动预选；填写示例并生成可复制预览；文本导出 URI 解码后与预览内容一致 |
| 控制台 | 通过 | 7 页布局验收时未发现浏览器 error 日志 |

### 已知限制与当前状态

- 内置浏览器没有返回下载事件，因此不将“文件实际落盘”记为通过；提供可直接选中复制的完整清单预览，导出内容已核验。
- 预约暂不对接后端或发送消息；文档、登录等旧辅助页面仍保留开发中提示，未放入新版主导航。
- 案例以历史阶段记录为依据，不新增上线时间、客户经营数据或效果指标。
- Vite 本地预览继续运行于 `http://127.0.0.1:3000/`；本次为本地官网更新，未部署线上。

## 2026-09-22：明确交付形式与三层可信

### 用户确认的补充口径

- 工业智能体平台：SaaS、私有化一体机两种交付形式。
- OfficeAI 国企办公：全部私有化交付。
- 共同定位：数据可信、Agent 可信、整套可信环境部署。
- 上轮已说明修改方案与验收重点，用户本轮要求继续执行。

### 本次完成

- 首页主标题升级为“让可信 AI 深入工业现场，走进国企办公”，首屏与两大业务卡片直接说明交付形式。
- 工业平台新增 SaaS / 私有化一体机对照，明确可信云环境与企业侧整套可信环境的部署位置及运营分工。
- OfficeAI 新增整套私有化部署说明，覆盖模型、数据与知识、技能、Agent 运行和管理组件；移除“评估私有化部署”的可选表述。
- 首页、工业平台、OfficeAI 共用三层可信说明：数据来源/版本/授权与引用；Agent 身份/技能/执行权限、过程记录与结果复核；整套环境部署、隔离、访问及运维边界。
- 关于我们、工业解决方案、页脚、网页描述和演示需求清单同步更新。
- 可信作为产品与交付要求表达；没有新增安全认证、硬件 TEE、绝对正确等未经确认的声明。本次修改为官网表达，不是对后端可信能力的实现或安全认证。

### 验收结果

- `npm run build`：通过（TypeScript、Vite）。
- 本次修改的 9 个 TS/TSX 文件 ESLint：通过；`git diff --check`：通过。
- 浏览器检查首页、工业平台、OfficeAI、工业解决方案、关于我们、演示准备共 6 页，分别使用 1440px 与 390px 视口：全部正常，无横向溢出，无浏览器 error 日志。
- 首页及两产品页均有“数据可信 / Agent 可信 / 环境可信”三张卡片；两产品页部署章节与用户要求一致。
- 目视检查手机首页、桌面两种交付卡片和 OfficeAI 可信章节：文字完整，无重叠遮挡。
- 演示清单实际生成验证：OfficeAI 显示“全部私有化部署”，工业平台显示“SaaS 或私有化一体机”，二者都写入三层可信要求。
- 恢复本地预览服务：`http://127.0.0.1:3000/`；仅更新本地官网，未部署线上。

## 2026-09-22：发布 GitHub 与原 Cloudflare 站点

### 发布目标与计划

- 用户明确要求推送 GitHub 并更新已有 Cloudflare 部署。
- 已核对 GitHub：`scottfromcn/zhipu-xinglian-website`，`main` 远端原提交 `5aa0280`，本地领先 3 个提交。
- 已核对 Cloudflare：已有 Pages 项目 `zhipu-xinglian`，生产分支 `main`，Direct Upload；绑定 `suzhouzp.top`、`www.suzhouzp.top`。
- 原生产部署：`a8fed14c-6a66-4790-a78a-29999a5d668b`，2026-05-29，状态 success。
- 发布前发现 Vite 使用相对资源路径；已将 `base` 改为 `/`，避免生产二级路由直达时错误请求 `/solutions/assets/`。
- 新增 `docs/DEPLOYMENT.md` 记录已有资源、构建、推送、手动部署与验收方式。
- 验收方案：构建和资源路径检查通过，推送后远端提交一致，Cloudflare 生产部署成功，正式域名首页及产品二级路由可加载且呈现新内容。

### 发布结果与验收

- 应用代码提交：`4b7edfb665bab1e5e0d97f5a18aa8234e36491e6`；已成功推送 GitHub `main`，包含前两轮业务与可信定位更新。
- 原 Cloudflare Pages 项目已更新生产部署，未新建项目；部署 ID：`f7204d3c-c425-46ad-82b7-6078490e2aa4`。
- 部署地址：`https://f7204d3c.zhipu-xinglian.pages.dev`；正式地址：`https://suzhouzp.top`。
- Cloudflare API 确认 canonical deployment 为上述部署，环境 production、状态 success、应用提交一致、工作区 dirty=false。
- `npm run build`、`npx eslint vite.config.ts`、`git diff --check` 均通过；构建资源使用 `/assets/` 根路径，SPA 回退文件已上传。
- HTTPS 验收：正式域名和 www 首页、`/industrial-platform`、`/solutions/industrial`、`/office-ai` 均返回 200；去除 Cloudflare 自动注入的访问统计脚本及空白差异后，HTML 与本次构建一致；JS/CSS 与本地构建逐字节一致。Pages 默认域名也已通过 HTML 与资源一致性验证。
- 部分代理请求发生 403 或 TLS 超时；改用正常浏览器 User-Agent 并通过直连复验后，上述正式域名及资源均通过；未修改任何安全配置。
- 浏览器已打开正式站点并返回新版标题，但本轮 AX/DOM 验收接口连续超时，因此不将线上视觉复验记为通过。上一轮本地桌面/手机视觉与交互验收已通过，本轮线上完成 HTTP、路由、构建资源一致性和部署状态验证。
- 本条发布结果作为文档提交另行推送；不改变已部署的应用代码，Cloudflare 对应应用提交仍为 `4b7edfb`。

## 2026-09-23：字体与风格参考研究

- 用户要求官网字体和风格学习 `https://bigmodel.cn/glm-coding`。
- 已读取工作日志和现有共享样式；内置浏览器超时后，使用 Chrome 原生界面实际查看首屏与卡片，并通过公开 CSS 确认字体栈、背景与文字色。
- 方案记录：`docs/VISUAL-REFRESH.md`，包含字体层级、灰白/黑色视觉、局部渐变、全站调整范围、执行顺序和桌面/手机验收标准。
- 遵循用户的“方案先讨论……后再执行”要求，当前处于方案确认节点；未修改应用样式或部署。
- 本阶段自验：方案中的主色与字体描述有实际页面和公开 CSS 依据；Git diff 空白检查通过。

## 2026-09-23：实施官网字体与视觉调整

### 已确认范围与实现

- 用户回复“执行”，授权按 `docs/VISUAL-REFRESH.md` 实施、验收并更新 GitHub 与原 Cloudflare 站点。
- 统一 `src/site.css` 的字体、颜色变量、按钮、卡片、页头、页脚、表单及响应式样式；移除首页背景图元素。
- 采用系统无衬线字体、浅灰白背景 `#f7f8fa`、近黑标题 `#131212`、中性灰正文、黑色主按钮；蓝紫色仅用于少量强调。
- 首屏标题目视校准为 400 字重，章节和卡片标题以 500 为主；首页增加留白，两大业务卡片使用白底、细边框和局部低饱和渐变；页尾改为浅色居中引导。
- 交付形式、三层可信、四模块及现有页面功能保持原业务口径；未引入远端字体依赖或复制参考站品牌素材。

### 本地生产构建验收

- `npm run build`、修改文件 ESLint、`git diff --check` 通过；CSS 解析与变量引用检查通过。
- 浏览器访问构建预览 `http://127.0.0.1:3000/`，对首页、工业平台、工业方案、OfficeAI、案例、关于我们、演示准备共 7 页分别检查 1440px 与 390px 视口：全部仅一个 h1，页面与视口等宽，主要标题、正文、卡片与表单未检测到横向溢出或裁切，未发现损坏图片或浏览器 error。
- 目视检查桌面/手机首页、双业务卡片与可信卡片、工业两种交付卡片、手机 OfficeAI 首屏及 SkillHub 卡片、手机清单表单、桌面页尾：字体层级、留白与浅色风格统一，内容清晰。
- 手机导航进入 OfficeAI 后自动关闭；首页工业入口和品牌返回链接通过。SkillHub 锚点定位约 104px，固定导航底部 73px，无遮挡。
- 键盘 Tab 可聚焦“跳至正文”，焦点为 3px 实线；核心正文/强调文字与三种常用浅色背景对比度均超过 4.5:1（最低 4.82:1）。
- OfficeAI 与工业两种需求清单实际生成通过，交付形式及可信要求正确；OfficeAI 下载链接解码内容与预览完全一致。未新增在线预约发送功能。
- 本次验收通过后发布；生产部署 ID 与线上结果在下方补记。

### 发布与线上复验

- 应用代码提交 `c0560d84725701a0d84f6ce235c910ca53829e6a` 已推送 GitHub `main`。
- 原 Pages 项目 `zhipu-xinglian` 生产部署成功：`762c4d3e-1c0c-4f8e-bb42-7d15ef02ad79`，地址 `https://762c4d3e.zhipu-xinglian.pages.dev`。
- Cloudflare API 确认该版本为 canonical production deployment，状态 success，代码提交对应 `c0560d8`，dirty=false。
- 正式域名首页、工业平台、工业方案、OfficeAI 直达页，以及 www 与 Pages 默认域名首页均返回 200；剔除 Cloudflare 统计注入脚本后 HTML 与本地构建一致，正式域名 JS/CSS 与构建文件逐字节一致。
- Chrome 原生浏览器打开 `https://suzhouzp.top/`，已目视确认新版系统字体、轻字重标题、浅灰背景、黑色按钮及首页内容生效。内置浏览器仅线上访问超时，本地生产构建验收完整通过；本次已用 Chrome 补齐正式首页的视觉复验。
- 本地生产预览保持在 `http://127.0.0.1:3000/`；线上官网 `https://suzhouzp.top/` 已更新。此发布记录为后续文档提交，不改变已发布应用代码。
